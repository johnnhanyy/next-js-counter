const NavBar = () => {
  return (
    <nav style={{
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
       
      }}>
      <div>
        <h1 className="logo">Johns web</h1>
      </div>
    </nav>
  );
};

export default NavBar;
